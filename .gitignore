# generated data: regenerate from seed instead of committing
data/**/generated/
# Iteration 6a: a saved custom scenario is box-local state, not source. Its data
# and benchmark artifacts are already covered above and below; the config is not.
data/scenarios/custom-*.yaml
data/.custom-staging/
benchmark/*.json
benchmark/*.csv
# generated benchmark reports (incl. suite-summary.md) — regenerate via `make bench-all`;
# the recorded numbers live in the committed docs (journal + iteration-2 handoff)
benchmark/*.md
.env
# model weights / artifacts
*.ckpt
*.pt
*.onnx
# python
__pycache__/
*.pyc
.venv/
venv/
# web
web/node_modules/
web/dist/
web/*.tsbuildinfo
# os
.DS_Store

# headless browser verification output (regenerate with `make web-check`)
web/e2e/shots/
web/e2e/node_modules/
web/e2e/package*.json
.phase3-shots/
